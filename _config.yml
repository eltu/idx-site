title: idx
description: Fast BM25 code search for Git repositories
baseurl: ""
url: "https://idx.cool"

exclude:
  - "**/.idx"
  - "idx/"

defaults:
  - scope:
      path: "features"
    values:
      layout: default
  - scope:
      path: "releases"
    values:
      layout: default
